name: protoexample
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  protobuf: ^3.0.0
  grpc: ^3.2.4
  grpc_server_stuff:
    path: ./gen
  flutter:
    sdk: flutter
  sprintf: ^7.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
